import styled from "styled-components";

const LogoComponent: React.FC = () => (
  <Logo>MONALISHA</Logo>
);

export default LogoComponent;

//styles
const Logo = styled.p`
 color:purple;
 font-size:1rem;
 font-weight:700;
`;
